/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['th.bing.com' , 'mir-s3-cdn-cf.behance.net' , "yui-marke.com"], // Add the domain of the external image
    },
  };
  
  export default nextConfig;